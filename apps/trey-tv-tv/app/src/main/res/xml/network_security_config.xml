<?xml version="1.0" encoding="utf-8"?>
<!--
  Trey TV TV-app network security config.

  Production posture: HTTPS only. No cleartext fallback. Default trust anchors
  (system CAs) are explicitly opted in.

  Dev cleartext domains must be top-level domain-config siblings. Android does
  not allow domain-config inside debug-overrides.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Dev-only cleartext to local Android/WebView test hosts. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
